{
  "name": "com.joshroman.macos-memory",
  "version": "1.0.0",
  "description": "Stream Deck plugin to monitor macOS memory and swap usage",
  "main": "com.joshroman.macos-memory.sdPlugin/bin/plugin.js",
  "scripts": {
    "build": "tsc --noEmit && esbuild src/plugin.ts --bundle --platform=node --format=cjs --outfile=com.joshroman.macos-memory.sdPlugin/bin/plugin.js --sourcemap",
    "watch": "npm run build -- --watch"
  },
  "author": "Josh Roman",
  "license": "MIT",
  "dependencies": {
    "@elgato/streamdeck": "^1.2.0"
  },
  "devDependencies": {
    "@rollup/plugin-commonjs": "^29.0.0",
    "@rollup/plugin-node-resolve": "^16.0.3",
    "@rollup/plugin-typescript": "^12.3.0",
    "@types/node": "^25.0.3",
    "esbuild": "^0.27.2",
    "rollup": "^4.54.0",
    "sharp": "^0.34.5",
    "tslib": "^2.8.1",
    "typescript": "^5.5.2"
  }
}
